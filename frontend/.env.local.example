# For local dev with FastAPI (python main.py in /backend):
NEXT_PUBLIC_API_URL=http://localhost:8000

# For Vercel deployment: leave this EMPTY (or delete this line entirely)
# The frontend calls /api/... on the same domain as the Next.js app
# NEXT_PUBLIC_API_URL=
